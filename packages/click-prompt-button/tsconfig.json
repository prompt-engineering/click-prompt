{
  "extends": "tsconfig/base.json",
  "baseUrl": ".", // This has to be specified if "paths" is.
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["**/*.ts", "**/*.tsx"]
}
